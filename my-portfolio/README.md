**Welcome to your project** 

**About**

View and edit your app locally.

This project contains everything you need to run your app locally.

**Edit the code in your local development environment**

Any change pushed to the repo will be reflected in your deployment workflow.

**Prerequisites:** 

1. Clone the repository using the project's Git URL 
2. Navigate to the project directory
3. Install dependencies: `npm install`
4. Create an `.env.local` file and set the right environment variables

```
VITE_APP_ID=your_app_id
VITE_APP_BASE_URL=your_backend_url

e.g.
VITE_APP_ID=cbef744a8545c389ef439ea6
VITE_APP_BASE_URL=https://my-backend.example.com
```

Run the app: `npm run dev`

**Publish your changes**

Follow your project's deployment process to publish changes.

**Docs & Support**

Documentation: See your project's docs or repository README for integration details.
